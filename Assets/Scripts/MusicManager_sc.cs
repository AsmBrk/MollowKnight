using UnityEngine;

public class MusicManager_sc : MonoBehaviour
{
    private static MusicManager_sc instance;

    void Awake()
    {
        if (instance == null)
        {
            instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}
